from collections import deque

class Graph:
    def __init__(self):
        self.nodes = []

class Node:
    def __init__(self, n_id, name):
        self.n_id = n_id
        self.name = name
        self.neighbors = []

def route_between_node(g, n1, n2):
    #use a simple BFS or DFS to search graph.

def minimal_binary_tree(arr, left, right):
    #medium in arr should always be the node.
    #left, right starts as 0 and len(arr)-1 and we recurse on each one.

def list_of_depths(b_tree):

def check_balanced_tree(tree):

